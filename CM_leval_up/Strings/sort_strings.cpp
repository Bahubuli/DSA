#include<bits/stdc++.h>
using namespace std;

string extracttoken(string str,int key)
{
    //string tokeniser
    
}


int main()
{
    int n;
    cin >> n;
    string temp;
    vector<string> v;
    for(int i=0;i<n;i++)
    {
        getline(cin, temp);
        v.push_back(temp);
    }
    int key;
    string reversal, ordering;
    cin >> key >> reversal >> ordering;
}
